var React = require('react');
var ReactDOM = require('react-dom');
var StatusList = require('./components/StatusList.react');

ReactDOM.render(
  <StatusList />,
  document.getElementById('react-container')
);
